CODE "?dup",0,qDup
    ret

CODE ".(",0,dotParen
    ret

CODE ".r",0,dotR
    ret

CODE "(",0,PAREN
    ret

CODE "*",0,star
    ret

CODE "*/",0,starSlash
    ret

CODE "*/mod",0,starSlashMod
    ret

CODE "/mod",0,slashMod
    ret

CODE "#",0,hash
    ret

CODE "#>",0,hashGT
    ret

CODE "#s",0,hashS
    ret

CODE "<#",0,LTHash
    ret

CODE "<>",0,notEQ
    ret

CODE ">in",0,GTIn
    ret

CODE "abort",0,abort
    ret

CODE "abort.Q",0,abortQT
    ret

CODE "abs",0,abs
    ret

CODE "accept",0,accept
    ret

CODE "cmove",0,cMove
    ret

code "constant",0,constant
    ret

code "decimal",0,decimal
    ret

code "dump",0,dump
    ret

code "emit",0,emit
    ret

code "erase",0,erase
    ret

code "evaluate",0,evaluate
    ret

code "exit",0,exit
    ret

code "fill",0,fill
    ret

code "fm/mod",0,FMSlashmod
    ret

code "hex",0,hex
    ret

code "hold",0,hold
    ret

code "invert",0,invert
    ret

code "is",0,is
    ret

code "literal",0,literal
    ret

code "m*",0,mstar
    ret

code "mod",0,mod
    ret

code "move",0,move
    ret

code "pad",0,pad
    ret

code "parse",0,parse
    ret

code "parse-name",0,parseName
    ret

code "pc!",0,pcStore
    ret

code "pc@",0,pcAt
    ret

code "pick",0,pick
    ret

code "postpone",0,postpone
    ret

code "quit",0,quit
    ret

code "recurse",0,recurse
    ret

code "roll",0,roll
    ret

code "rp!",0,rpStore
    ret

code "rp@",0,rpAt
    ret

code "s.q",0,sq
    ret

code "s>d",0,stod
    ret

code "see",0,see
    ret

code "sign",0,sign
    ret

code "sp!",0,spStore
    ret

code "sp@",0,spAt
    ret

code "space",0,space
    ret

code "spaces",0,spaces
    ret

code "to",0,to
    ret

code "type",0,type
    ret

code "u.",0,udot
    ret

code "u.r",0,uDotR
    ret

code "u>",0,uGT
    ret

code "um/mod",0,umSlashMod
    ret

code "um+",0,umPlus
    ret

code "unused",0,unused
    ret

code "value",0,value
    ret

code "variable",0,variable
    ret

code "within",0,within
    ret

code "words",0,words
    ret
